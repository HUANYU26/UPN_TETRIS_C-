#include "position.h"

// constructor inicializa un objeto Position y Representa la fila y columna de la posición 
Position::Position(int row, int column)
{
    this -> row = row;//Asigna el valor del parámetro row al atributo row del objeto actual
    this -> column = column;//Asigna el valor del parámetro column al atributo column del objeto actual

}