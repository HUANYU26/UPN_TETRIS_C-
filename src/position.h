#pragma once

//Declaración de la clase position
class Position
{
    //Inicializa un objeto Position con los valores especificados para la fila (row) y la columna (column)
public:
    Position(int row, int column);
    int row; //Entero que representa la fila de la posición
    int column;//Entero que representa la columna de la posición

};